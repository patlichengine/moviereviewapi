import sql, { SQLQuery, isSqlQuery } from '@databases/sql';
import { Options as DatabaseOptions } from 'better-sqlite3';
export type { SQLQuery };
export { sql, isSqlQuery };
export interface DatabaseTransaction {
    query(query: SQLQuery | SQLQuery[]): any[];
    queryStream(query: SQLQuery): IterableIterator<any>;
}
export interface DatabaseConnection extends DatabaseTransaction {
    tx<T>(fn: (db: DatabaseTransaction) => T): T;
    dispose(): void;
}
export declare const IN_MEMORY = ":memory:";
export default function connect(filename?: string, options?: DatabaseOptions): DatabaseConnection;
